<UserControl x:Class="LightShell.IssueFields.Search.SearchBySprintFieldControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightShell.IssueFields.Search"
             mc:Ignorable="d" d:DesignWidth="300" Height="70">
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="Auto"/>
         <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Row="0" Grid.Column="0" Text="Select board:" Margin="5"/>
      <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding BoardsList}" SelectedItem="{Binding SelectedBoard, Mode=TwoWay}"
                      Margin="5" DisplayMemberPath="Name"/>
      <TextBlock Grid.Row="1" Grid.Column="0" Text="Select sprint:" Margin="5"/>
      <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding SprintsList}" SelectedItem="{Binding SelectedSprint, Mode=TwoWay}"
                      Margin="5" DisplayMemberPath="Name"/>
   </Grid>
</UserControl>
